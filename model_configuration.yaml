
# Model configuration file 

model_type: yolov8n.pt 
pretrained: True
seed: 0
imgsz: [640, 480] 
batch: 4 # number of samples processed at once during training
epochs: 1
optimizer: SGD 
learning_rate: 0.01  # how weights are updated during training
# project: "./logs"  # If you want to log files into different directory than ./runs/detect  
project: "./mlruns"  # If you want to log files into different directory than ./runs/detect  


# MLFlow parameters
experiment_name: 'YOLOv8'
experiment_run: 'train'